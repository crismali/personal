{
  "name": "personal",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run dev.ts",
    "build": "bun run build.ts",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "prettier": "^3.8.4",
    "sass": "^1.101.0",
    "typescript": "^6.0.3"
  }
}
